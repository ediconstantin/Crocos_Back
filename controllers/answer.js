const Answer = require('../models').Answer;

//endpoint to save answer
//with parameter for getting feedback
//with parameter for force-closing the user-session

//user-session service to check if a user can save an answer
module.exports.updateAnswer = async (ctx) => {
    
}